#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include "main.h"
/**
 * _abs - Entry point.
 * @n: abs
 * Return: Always 0 (Success)
 */
int _abs(int n)
{

	if (n >= 0)
		return (n);
	else
		return (-n);

}
